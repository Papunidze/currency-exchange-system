import Popover from './popover';
import {
  PopoverProps,
  PopoverSize,
  PopoverVariant,
  PopoverPlacement,
  PopoverTrigger,
} from './popover.inerfaces';

export {
  Popover,
  type PopoverProps,
  type PopoverSize,
  type PopoverVariant,
  type PopoverPlacement,
  type PopoverTrigger,
};

export default Popover;
